# BiolNexo — Env (copiar a .env o .env.local para local)
# Vite expone VITE_* ; NEXT_PUBLIC_* es compat Next (se mapea en src/lib/supabase.ts)

# --- Supabase Backend (nicho 4 slugs) — tus credenciales ---
VITE_SUPABASE_URL=https://pnkdymnrgfjifowlmlpl.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_kcHiDHKzgUB1hoa5MRyuiw_naR2jbKG
NEXT_PUBLIC_SUPABASE_URL=https://pnkdymnrgfjifowlmlpl.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_kcHiDHKzgUB1hoa5MRyuiw_naR2jbKG
VITE_SITE_URL=https://biolnexo.vercel.app # usado para magic-link redirect (si no, usa window.location.origin)
# Para Vercel: añade VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY + VITE_SITE_URL en Project → Settings → Environment Variables → Redeploy

# Solo servidor (no exponer en front): para scripts/agent_draft.py inserts y migrations
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_... (obtén en Supabase → Project Settings → API → service_role)

# --- Agente IA asistido 3×/semana ES/EN (apagado por defecto) ---
LLM_ENABLED=false
GEMINI_API_KEY= # aistudio.google.com → Create API key (free 60 req/min)
GEMINI_MODEL=gemini-2.0-flash
VITE_LLM_ENABLED=false

