# BiolNexo — Env (copiar a .env o .env.local para local)
# Vite sólo expone VITE_*

# --- Supabase Backend (nicho 4 slugs) ---
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ... # solo en servidor / Edge Functions, nunca en front
# Para Vercel: añade estas 3 en Project → Settings → Environment Variables

# --- Agente IA asistido 3×/semana ES/EN (apagado por defecto) ---
LLM_ENABLED=false
GEMINI_API_KEY= # aistudio.google.com → Create API key (free 60 req/min)
GEMINI_MODEL=gemini-2.0-flash
VITE_LLM_ENABLED=false

# Cron: lunes/miércoles/viernes 06:00 UTC → scripts/agent_draft.py
# Fase A sin LLM solo recolecta DOIs en sources + drafts pending_review

